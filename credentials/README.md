# `credentials/`

Credential type definitions. Each subfolder contains the schema, OAuth redirect handlers, refresh logic, and validation rules for one credential type.

## Planned contents

- `oauth`
- `api-key`
- `jwt`
- `basic-auth`
- `cloud`
- `enterprise`
